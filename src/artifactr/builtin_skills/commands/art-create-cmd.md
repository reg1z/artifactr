---
description: Create a new Artifactr command.
---

Run `art create command/<name>` (or `art create cmd/<name>`) to create a command in the default vault. Use `-v <vault>` to target a specific vault, `--edit` to open in editor immediately.
