---
description: Create a new Artifactr agent.
---

Run `art create agent/<name>` (or `art create agt/<name>`) to create an agent in the default vault. Use `-v <vault>` to target a specific vault, `--edit` to open in editor immediately.
